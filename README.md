"# ZillowMonitor" 

build database
	Tables
		listing
			listingID
			url
			lat, lon
		Status history
			listingID
			date
			status (e.g. under contract)
			price
	
write scraper

schedule scraper to run nightly